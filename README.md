# 🐺 Big Bad Wolf — Slot Machine

A browser-based, cowboy-themed slot machine inspired by the "huff-and-puff"
genre: a 5×3, **243-ways** game with a Hard-Hat Free-Spins bonus where the Big
Bad Wolf blows down straw / stick / brick houses for prizes, plus a Mansion
Jackpot. Pure vanilla HTML/CSS/JS — no framework, no runtime dependencies.

---

## Quick start

**Just play it:** open `index.html` in a browser (double-click works — it runs
straight off the `file://` protocol).

**Local web server** (needed for the in-game High-Noon easter egg's autoplay and
for a deploy-like environment):

```bash
node tools/serve.js        # serves the folder at http://localhost:8123
```

**After changing any source file under `src/`, rebuild the bundle:**

```bash
node tools/build.js        # bundles src/ → app.js
```

The browser only ever loads the single generated **`app.js`** — see *How the
build works* below.

---

## Project structure

```
.
├── index.html             The page. Loads app.js + styles.css.
├── app.js                 GENERATED bundle of src/ — the only script the page loads.
├── styles.css             All styling.
├── netlify.toml           Deploy config: blocks local-only files from being served.
├── README.md              You are here.
│
├── src/                   ── ALL SOURCE (ES modules, bundled into app.js) ──
│   ├── main.js              Entry point: imports everything, runs init().
│   │
│   ├── math/                THE MATH (start here to understand payouts)
│   │   ├── par-sheet.js       The par sheet: symbols, paytable, reel strips,
│   │   │                      bonus config, RTP models, constants. One source of truth.
│   │   └── mathcore.js        Pure math: 243-ways evaluation + bonus award rolls.
│   │                          No DOM — imported by both the game AND tools/sim.js.
│   │
│   ├── core/                state.js (shared runtime state) · utils.js (helpers)
│   ├── audio/               sound.js (sfx + music) · narrator.js · phrases.js (wolf script)
│   ├── render/              reels.js (reel animation) · particles.js · readouts.js (HUD)
│   ├── game/                base-game.js · bonus.js · buy-bonus.js
│   ├── panels/              The drawer pop-ups:
│   │                          options-drawer · deposit · rtp-picker · game-size ·
│   │                          simulator (📊 SIM) · math-breakdown (🧮 MATH) ·
│   │                          size-manifest (GENERATED by build.js)
│   ├── scenes/              Cinematic / ambient: intro · reveal · day-night ·
│   │                          high-noon (easter egg) · idle-poster
│   └── system/              dev-mode.js (hides admin tools on the public build)
│
├── tools/                 ── DEV SCRIPTS (Node, never shipped to the browser) ──
│   ├── build.js             Bundle src/ → app.js (+ regenerate the size manifest).
│   ├── sim.js               Headless Monte-Carlo RTP verifier.
│   ├── serve.js             Tiny static file server for local testing.
│   ├── voice.js             Generate the narrator clips via ElevenLabs.
│   ├── music.js             Generate the base/bonus music via ElevenLabs.
│   └── process_images.py    One-off helper: knock out white image backgrounds.
│
├── docs/
│   └── PARSHEET.md          Human-readable par sheet + RTP breakdown.
│
└── assets/                Media (images, video, audio) — gitignored, not in version control.
    ├── audio/ {music, sfx, narrator}
    ├── webm/              transparent + cutscene videos
    └── *.png, bg.jpg      symbols + background
```

---

## How the build works

Browsers block native ES-module `import` over the `file://` protocol, so the
clean module source in `src/` is bundled into one plain classic script, `app.js`,
by `tools/build.js`. Each module is wrapped in its own scope with a tiny
CommonJS-style `require()`, and imports are resolved by the module's **basename**
(so `import '../math/par-sheet.js'` becomes `require("par-sheet")`). That means
you can freely move files between folders without breaking the build — the only
rule is **basenames must stay unique** across `src/` (the build throws if not).

`index.html` loads only `app.js`, so the entire `src/` tree and `tools/` are
purely for development — never required at runtime by the browser.

---

## The math (and how to verify it)

Everything that decides money lives in two files, both heavily commented:

- **`src/math/par-sheet.js`** — the par sheet. Symbol paytable (per-way
  multipliers), per-reel symbol counts, the bonus award table, and named
  constants. Change a number here and the whole game changes.
- **`src/math/mathcore.js`** — the pure logic: how 243-ways pays are computed and
  how bonus awards roll. No DOM, no randomness hidden anywhere else.

A plain-English walkthrough lives in **`docs/PARSHEET.md`**, and the same numbers
power the in-game **🧮 MATH** and **📊 SIM** panels.

**Verify the real return-to-player with a Monte-Carlo run:**

```bash
node tools/sim.js 10000000      # 10M spins
```

> **Note on current RTP:** the par sheet was originally tuned to ~97% RTP, but it
> currently measures **~102%** total (base ≈ 59%, bonus ≈ 43%, bonus trigger
> ≈ 1 in 177). The numbers drifted above target at some point; re-tuning is a
> product decision, so the code/docs report the *actual* figures rather than the
> old target. To bring it back to 97%, lower base-game pays and/or bonus award
> magnitudes in `par-sheet.js` and re-run the sim until it converges.

---

## Developer / admin tools

The **"GAFF MODE"** tools — RTP picker, GAME SIZE, Add-Credit, and the
📊 SIM / 🧮 MATH debug panel — are **hidden on the public build** and grouped
under a "GAFF MODE" header in the options drawer. Enable them by adding `?dev=1`
to the URL once (the choice is remembered); `?dev=0` turns them back off. The
backtick (`` ` ``) key toggles the SIM/MATH panel when dev mode is on.

(Buy Bonus is a normal player feature, so it stays visible in regular mode.)

---

## Deploying

Drag the folder to a static host (e.g. Netlify) or connect the repo. `netlify.toml`
adds defense-in-depth: it returns 404 for `/.env`, `/tools/*`, `/.git/*`, etc., so
nothing local-only is ever served even if the whole folder is uploaded. The
media in `assets/` is gitignored — re-upload it with the deploy.

There's also a hidden surprise at exactly 12:00 PM local time. 🤠
